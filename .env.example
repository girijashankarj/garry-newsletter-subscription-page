# Same variable as portfolio — same Apps Script / sheet.
# Deploy NewsletterCode.gs as Web App (Execute as Me, Anyone).
VITE_NEWSLETTER_SCRIPT_URL=https://script.google.com/macros/s/YOUR_NEWSLETTER_DEPLOYMENT_ID/exec
